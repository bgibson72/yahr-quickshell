import QtQuick
import Quickshell

IconButton {
    icon: ""
    tooltip: "Files"
    onClicked: Quickshell.execDetached(["thunar"])
}import Quickshell

IconButton {
    icon: ""
    tooltip: "Files"
    onClicked: Quickshell.execDetached(["thunar"])
}tQuick
import Quickshell

IconButton {
    icon: ""
    tooltip: "Files"
    onClicked: Quickshell.execDetached("nautilus")
}
